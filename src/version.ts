/**
 * Version of this SDK. It is part of the outgoing `User-Agent` header and the
 * public API surface.
 *
 * Keep in sync with `package.json`; `test/version.test.ts` fails otherwise.
 */
export const SDK_VERSION = "1.0.0";
